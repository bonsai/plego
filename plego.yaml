global:
  timezone: Asia/Tokyo
  state_db: ~/.plego/state.db

plugins:
  - module: Subscription::Filesystem
    config:
      path: G:/My Drive/wiki
      extensions: [.md, .txt]

  - module: Publish::Gmail
    config:
      to: onsen.bonsai@gmail.com
      credentials: ./credentials.json
