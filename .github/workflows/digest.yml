# このファイルは廃止。digest-v0.yml / digest-v1.yml を使用してください。
# 残しているのは既存の参照が壊れないようにするためです。
name: Digest (deprecated – see digest-v0.yml)
on:
  workflow_dispatch:
jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use digest-v0.yml or digest-v1.yml instead."
